/** Update before App Store / Play submission if this address changes. */
export const SUPPORT_EMAIL = 'support@pickleballcx.com';

export const supportMailtoUrl = `mailto:${SUPPORT_EMAIL}` as const;
